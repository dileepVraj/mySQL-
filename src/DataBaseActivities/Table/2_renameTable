In this section we will get to know how to rename a table in database.

========================================================================================================================

1. How to rename table name in mySQL:
   ---------------------------------
   To rename a table name use 'RENAME table OldTableName TO newTableName'.
   The above command will rename a table name in mySQL.
========================================================================================================================